
#include "server.h"

int main(void)
{
	volatile int ser_sock = 0;
	volatile struct sockaddr cli_addr = {0};
	volatile char r_buff[] = "\0";
	const char w_buff[] = "hello client";
	const socklen_t = sizeof(struct sockaddr);	
	

	return 0;
}
